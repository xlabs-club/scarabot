# Markdown lint rules
default: true
# Allow long lines (Chinese prose and code blocks exceed 80 chars)
MD013: false
# Allow inline HTML (<div align="center"> in README headers)
MD033: false
# Allow first line to not be a top-level heading (READMEs start with <div align="center">)
MD041: false
